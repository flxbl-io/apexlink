import { Logger, LoggerLevel } from '@flxbl-io/sfp-logger';
export default class ExecuteCommand {
    protected logger?: Logger;
    protected logLevel?: LoggerLevel;
    protected showProgress?: boolean;
    constructor(logger?: Logger, logLevel?: LoggerLevel, showProgress?: boolean);
    execCommand(command: string, workingdirectory: string, timeout?: number, isEnableErrorFromOutputStream?: boolean): Promise<any>;
}
//# sourceMappingURL=ExecuteCommand.d.ts.map